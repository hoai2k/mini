# Site images

Source images for the pages under `sites/`, as delivered. Sites do not load
these directly; each keeps web-sized copies (WebP, cropped portraits, icons) in
its own `assets/` folder.

- `space_tiber.png` — the band portrait of Space Tiber, 1448 × 1086, used by
  `sites/space-tiber/`.
